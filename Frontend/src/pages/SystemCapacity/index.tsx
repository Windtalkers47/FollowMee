import { useEffect, useState } from 'react';
import { Alert, Button, Card, CardContent, Chip, LinearProgress, Stack, Typography } from '@mui/material';
import { API_URL } from '../../utils/runtimeEnv';
import { PageHeader, PageLoading, PageShell } from '../../components/PageState';
import { useUserPreferences } from '../../contexts/UserPreferencesContext';
type Metric = { resource: string; used: number | null; limit: number | null; unit: string; percent: number | null; exact: boolean };
type Provider = { provider: string; status: 'healthy'|'warning'|'critical'|'unknown'; dashboardUrl?: string; metrics: Metric[]; note?: string };
export default function SystemCapacityPage() { const { t } = useUserPreferences(); const [data, setData] = useState<{ checkedAt: string; providers: Provider[] } | null>(null); const [error, setError] = useState(false); const load = () => fetch(`${API_URL}/system/capacity`, { credentials: 'include' }).then(r => { if (!r.ok) throw new Error(); return r.json(); }).then(p => { setData(p.data); setError(false); }).catch(() => setError(true)); useEffect(() => { void load(); const timer = setInterval(load, 300_000); return () => clearInterval(timer); }, []); return <PageShell><PageHeader title={t('uat.capacity.title')} subtitle={t('uat.capacity.subtitle')} />{error && <Alert severity="error">{t('uat.capacity.error')}</Alert>}{!data ? <PageLoading rows={3} label={t('uat.capacity.loading')} /> : <Stack gap={2}>{data.providers.map(provider => <Card variant="outlined" key={provider.provider} sx={{ borderRadius: 3 }}><CardContent><Stack direction="row" justifyContent="space-between"><Typography variant="h6" textTransform="capitalize">{provider.provider}</Typography><Chip label={provider.status} color={provider.status === 'critical' ? 'error' : provider.status === 'warning' ? 'warning' : provider.status === 'healthy' ? 'success' : 'default'} /></Stack>{provider.metrics.map(metric => <Stack key={metric.resource} gap={.5} mt={2}><Stack direction="row" justifyContent="space-between"><Typography>{metric.resource}</Typography><Typography>{metric.percent == null ? t('uat.capacity.providerOnly') : `${metric.percent.toFixed(1)}%`}</Typography></Stack>{metric.percent != null && <LinearProgress variant="determinate" value={Math.min(metric.percent, 100)} color={metric.percent >= 95 ? 'error' : metric.percent >= 70 ? 'warning' : 'primary'} />}<Typography variant="caption" color="text.secondary">{metric.used ?? '—'} / {metric.limit ?? '—'} {metric.unit} · {t(metric.exact ? 'uat.capacity.verified' : 'uat.capacity.provider')}</Typography></Stack>)}{provider.metrics.some(metric => !metric.exact) && <Alert severity="info" sx={{ mt: 2 }}>{t('uat.capacity.providerOnly')}</Alert>}{provider.dashboardUrl && <Button href={provider.dashboardUrl} target="_blank" rel="noreferrer" sx={{ mt: 1 }}>{t('uat.capacity.openDashboard')}</Button>}</CardContent></Card>)}</Stack>}</PageShell>; }
